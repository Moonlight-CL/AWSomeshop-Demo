# 生产环境配置示例
# 在 ECS 部署时，这些环境变量应该通过以下方式提供：
# 1. ECS 任务定义中的环境变量
# 2. AWS Systems Manager Parameter Store
# 3. AWS Secrets Manager
# 4. .env 文件挂载到容器中

# 应用配置
DEBUG=false
SECRET_KEY=your-production-secret-key-change-this-to-a-strong-random-value
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# 数据库配置 (使用您自己的 RDS 或外部数据库)
DATABASE_URL=postgresql+asyncpg://username:password@your-rds-endpoint:5432/awsome_shop
DATABASE_POOL_SIZE=10
DATABASE_MAX_OVERFLOW=20

# Redis配置 (可选,当前未使用)
# REDIS_URL=redis://your-redis-endpoint:6379

# CORS配置
ALLOWED_ORIGINS=["https://yourdomain.com"]
ALLOWED_HOSTS=["yourdomain.com","*.yourdomain.com"]

# 邮件配置 (可选)
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_USE_TLS=true
EMAIL_ENABLED=false

# AWS配置 (ECS 任务角色会自动提供权限,无需手动配置密钥)
AWS_REGION=us-east-1
S3_BUCKET_NAME=awsome-shop-assets-prod-<your-account-id>

# 文件上传配置
MAX_FILE_SIZE=5242880
ALLOWED_IMAGE_TYPES=["image/jpeg","image/png","image/webp"]
