# API Base URL
#
# Development (本地开发):
#   设置为后端服务地址，例如 http://localhost:8000
#   前端将直接连接此 URL
#
# Production (生产环境):
#   设置为空字符串（推荐）使用相对路径
#   前端通过 ALB 访问，ALB 会自动路由 /api/* 请求到后端
#   或者设置为完整的 ALB URL，例如 http://your-alb-url.com
#
VITE_API_URL=http://localhost:8000
